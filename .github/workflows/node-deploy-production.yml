name: Deploy Production CI

on:
  push:
    branches: [ production ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the Docker image
      run: docker build . --file ./docker/vendoor-server.dockerfile --tag vendoor/server:latest
